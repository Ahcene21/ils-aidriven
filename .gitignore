# OJS compiles the theme's LESS into its own cache directory; nothing in this
# repository is generated at build time.
.DS_Store
Thumbs.db
node_modules/
*.log

# The packaged theme is built from wordpress/mkadmi/ with `zip -r mkadmi.zip mkadmi`.
wordpress/*.zip
